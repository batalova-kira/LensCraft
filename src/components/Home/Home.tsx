import { ImageSlider } from "../ImageSlider/ImageSlider";

export const Home = (): JSX.Element => {
    return (
        <section id="home">
            <ImageSlider />
        </section>
    );
};
