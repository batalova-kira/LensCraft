export const Contacts = (): JSX.Element => {
    return (
        <section id="contacts">
            <h1>Contact Section</h1>
            <p>Contact us for more information!</p>
        </section>
    );
};
